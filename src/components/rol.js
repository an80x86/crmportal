import React from 'react';

import RolContainer from '../containers/rol_container';

const Rol = () =>{
    return (
        <div>
            <RolContainer/>
        </div>
    )
}

export default Rol;
